# =============================================================================
# HyperSense Docker Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values:
#   cp .env.docker.example .env
#
# Then start the application:
#   docker compose up -d --build
#
# Access at: http://localhost
# =============================================================================

# =============================================================================
# Database Configuration
# =============================================================================
POSTGRES_USER=hypersense
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=hypersense_production

# =============================================================================
# Rails Configuration
# =============================================================================
# REQUIRED: Secret key for session encryption and signed cookies
# Generate with: openssl rand -hex 64
SECRET_KEY_BASE=

# =============================================================================
# LLM Provider Configuration
# =============================================================================
# Options: anthropic, gemini, ollama, openai
LLM_PROVIDER=anthropic

# Anthropic (required if LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your_anthropic_api_key
ANTHROPIC_MODEL=claude-haiku-4-5

# Gemini (required if LLM_PROVIDER=gemini)
#GEMINI_API_KEY=your_gemini_api_key
#GEMINI_MODEL=gemini-2.5-flash

# OpenAI (required if LLM_PROVIDER=openai)
#OPENAI_API_KEY=your_openai_api_key
#OPENAI_MODEL=gpt-5.2

# Ollama (required if LLM_PROVIDER=ollama)
# Note: Use host.docker.internal to access Ollama running on the host machine
#OLLAMA_API_BASE=http://host.docker.internal:11434/v1
#OLLAMA_MODEL=qwen3:8b

# =============================================================================
# Hyperliquid Account
# =============================================================================
# ADDRESS: Set to view your real account balance (works in paper trading mode)
# PRIVATE_KEY: Only required for live trading (leave empty for paper trading)
HYPERLIQUID_ADDRESS=
HYPERLIQUID_PRIVATE_KEY=

# =============================================================================
# Mission Control Dashboard (optional)
# =============================================================================
# HTTP Basic Auth for /jobs dashboard
MISSION_CONTROL_USER=admin
MISSION_CONTROL_PASSWORD=your_secure_admin_password

# =============================================================================
# Performance Tuning (optional)
# =============================================================================
# Number of concurrent job workers (default: 2)
JOB_CONCURRENCY=2

# Frontend exposed port (default: 80)
# Change if port 80 is already in use
FRONTEND_PORT=80
